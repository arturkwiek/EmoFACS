# Virtual environments — one per platform (.venv-win, .venv-wsl, ...)
.venv/
.venv-*/

# Python cache
__pycache__/
*.pyc

# Personal mood data — do not commit
data/

# Trained weights (large binaries)
models/*.pth

# Local helper folders for files pending deletion
_to_delete/
_to_delete_*/

# OS / editor noise
Thumbs.db
.DS_Store
